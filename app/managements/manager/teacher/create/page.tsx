import FormHelpersAdd from "@/helpers/dynamic/FormTeachers";

const CreatePage = () => {
  return (
    // tipe form dinamis atau ada data dari database yang perlu di display di form
    <FormHelpersAdd />
    // tipe form dinamis atau ada data dari database yang perlu di display di form
  );
};

export default CreatePage;
